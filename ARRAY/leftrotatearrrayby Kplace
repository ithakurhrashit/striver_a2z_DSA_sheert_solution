class Solution {
public:
    void rotateArrayByOne(vector<int>& nums) {
        int temp =nums[0];
        for(int i=1;i<=nums.size();i++){
            if(i==nums.size()){
                nums[i]=temp;
            }
            
            nums[i-1]=nums[i];
            
        }

    }
};
